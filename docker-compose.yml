version: '3.8'

services:
  energy-monitor:
    build: .
    container_name: energy-monitor
    ports:
      - "8081:8081"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/energy.db
      - PORT=8081
      - TZ=Europe/Stockholm
    restart: unless-stopped
